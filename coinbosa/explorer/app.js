const I18N={
 fr:{status:'Statut',confirmed:'Confirmée',failed:'Échouée',fee:'Frais payés',timestamp:'Horodatage',rawData:'Données brutes',notFound:'Introuvable sur cette chaîne',sealer:'Scellé par',position:'Position dans le bloc',label:'Français',locale:'fr',explorer:'Explorer',footerDesc:'Explorateur de blocs, de transactions et de contrats de Coinbosa Chain.',ecosystem:'Écosystème',community:'Communauté',resources:'Ressources',readWhitepaper:'Lire le livre blanc',website:'Site officiel',deployer:'Déployeur',tokensLead:'Les jetons créés par les utilisateurs via le standard BRC20 apparaissent ici.',symbol:'Symbole',heroTitle:'Explorateur de la blockchain Coinbosa',search:'Rechercher',searchPlaceholder:'Bloc, hash de transaction, ou adresse 0x…',overview:'Aperçu',blocks:'Blocs',contracts:'Contrats',tokens:'Tokens',analytics:'Analytics',defi:'DeFi',online:'en ligne',offline:'hors ligne',stalled:'arrêtée ({n} s sans bloc)',supply:'Offre BOSA',decimals:'Décimales',emission:'Émission',none:'aucune',latestBlock:'Dernier bloc',blockTime:'Temps de bloc',transactions:'Transactions',inLastBlock:'Dans le dernier bloc',gasPrice:'Prix du gas',gas:'Gas',validators:'Validateurs',finality:'Finalité',probabilistic:'probabiliste',target:'Cible',blockTimeChart:'Temps de bloc',txPerBlock:'Transactions par bloc',cumulativeTx:'Transactions cumulées',gasTrend:'Prix du gas',latestBlocks:'Derniers blocs',latestTxs:'Dernières transactions',noTxs:'Aucune transaction pour l’instant',viewAll:'Tout voir',validator:'Validateur',active:'actif',txs:'tx',txsCol:'Tx',transfer:'Transfert',call:'Appel',contractCreation:'Création de contrat',searching:'Recherche…',error:'Erreur',copied:'Copié',notConnected:'Aucun nœud public raccordé pour le moment. Les blocs et transactions apparaîtront dès le raccordement du réseau.',age:'Âge',txHash:'Hash',method:'Action',from:'De',to:'Vers',value:'Valeur',gasUsed:'Gas utilisé',systemContracts:'Contrats système',deployedContracts:'Contrats déployés récemment',address:'Adresse',codeSize:'Taille du code',role:'Rôle',brc20Note:'Les applications déploient leurs jetons via le standard BRC20 de Coinbosa, compatible ERC-20.',nativeCoinT:'Coin natif',tokenN:'Jeton',typeC:'Type',native:'natif',brc20Tokens:'Jetons BRC20',noTokens:'Aucun jeton BRC20 déployé détecté. Ils apparaîtront ici dès qu’une application en déploie.',valNote:'Le réseau vise 12 validateurs identifiés. La couche d’enjeu (staking) et la finalité rapide sont en cours de développement.',comingSoon:'à venir',defiDesc:'La place d’échange de l’écosystème Coinbosa — marché au comptant et contrats à terme — est en construction. Les marchés, la liquidité et les paires apparaîtront ici une fois NextFuture raccordé à la chaîne. Aucun chiffre n’est affiché tant qu’il n’est pas réel.',block:'bloc',transaction:'transaction',balance:'solde',nonce:'nonce',kind:'genre',contract:'contrat',externalAccount:'compte externe',bytecodeSize:'taille du bytecode',bytes:'octets',unrecognized:'Format non reconnu',footer:'réseau ouvert, public, à validateurs autorisés'},
 en:{status:'Status',confirmed:'Confirmed',failed:'Failed',fee:'Fee paid',timestamp:'Timestamp',rawData:'Raw data',notFound:'Not found on this chain',sealer:'Sealed by',position:'Position in block',label:'English',locale:'en',explorer:'Explorer',footerDesc:'Block, transaction and contract explorer for Coinbosa Chain.',ecosystem:'Ecosystem',community:'Community',resources:'Resources',readWhitepaper:'Read the whitepaper',website:'Official website',deployer:'Deployer',tokensLead:'Tokens created by users via the BRC20 standard appear here.',symbol:'Symbol',heroTitle:'Coinbosa Blockchain Explorer',search:'Search',searchPlaceholder:'Block, transaction hash, or address 0x…',overview:'Overview',blocks:'Blocks',contracts:'Contracts',tokens:'Tokens',analytics:'Analytics',defi:'DeFi',online:'online',offline:'offline',stalled:'stalled ({n}s without a block)',supply:'BOSA supply',decimals:'Decimals',emission:'Emission',none:'none',latestBlock:'Latest block',blockTime:'Block time',transactions:'Transactions',inLastBlock:'In last block',gasPrice:'Gas price',gas:'Gas',validators:'Validators',finality:'Finality',probabilistic:'probabilistic',target:'Target',blockTimeChart:'Block time',txPerBlock:'Transactions per block',cumulativeTx:'Cumulative transactions',gasTrend:'Gas price',latestBlocks:'Latest blocks',latestTxs:'Latest transactions',noTxs:'No transactions yet',viewAll:'View all',validator:'Validator',active:'active',txs:'txs',txsCol:'Txs',transfer:'Transfer',call:'Call',contractCreation:'Contract creation',searching:'Searching…',error:'Error',copied:'Copied',notConnected:'No public node is connected yet. Blocks and transactions will appear as soon as the network is connected.',age:'Age',txHash:'Hash',method:'Action',from:'From',to:'To',value:'Value',gasUsed:'Gas used',systemContracts:'System contracts',deployedContracts:'Recently deployed contracts',address:'Address',codeSize:'Code size',role:'Role',brc20Note:'Applications deploy their tokens via Coinbosa’s BRC20 standard, ERC-20 compatible.',nativeCoinT:'Native coin',tokenN:'Token',typeC:'Type',native:'native',brc20Tokens:'BRC20 tokens',noTokens:'No deployed BRC20 token detected. They will appear here as soon as an application deploys one.',valNote:'The network targets 12 identified validators. The staking layer and fast finality are under development.',comingSoon:'coming soon',defiDesc:'The Coinbosa ecosystem exchange — spot and futures markets — is under construction. Markets, liquidity and pairs will appear here once NextFuture is connected to the chain. No figure is shown until it is real.',block:'block',transaction:'transaction',balance:'balance',nonce:'nonce',kind:'kind',contract:'contract',externalAccount:'external account',bytecodeSize:'bytecode size',bytes:'bytes',unrecognized:'Unrecognized format',footer:'open, public, validator-authorized network'},
 es:{status:'Estado',confirmed:'Confirmada',failed:'Fallida',fee:'Comisión',timestamp:'Fecha',rawData:'Datos brutos',notFound:'No encontrado',sealer:'Sellado por',position:'Posición',label:'Español',locale:'es',explorer:'Explorador',footerDesc:'Explorador de bloques, transacciones y contratos de Coinbosa Chain.',ecosystem:'Ecosistema',community:'Comunidad',resources:'Recursos',readWhitepaper:'Leer el libro blanco',website:'Sitio oficial',deployer:'Desplegador',tokensLead:'Los tokens creados por los usuarios con el estándar BRC20 aparecen aquí.',symbol:'Símbolo',heroTitle:'Explorador de la blockchain Coinbosa',search:'Buscar',searchPlaceholder:'Bloque, hash de transacción o dirección 0x…',overview:'Resumen',blocks:'Bloques',contracts:'Contratos',tokens:'Tokens',analytics:'Analytics',defi:'DeFi',online:'en línea',offline:'desconectado',stalled:'detenida ({n} s sin bloque)',supply:'Suministro BOSA',decimals:'Decimales',emission:'Emisión',none:'ninguna',latestBlock:'Último bloque',blockTime:'Tiempo de bloque',transactions:'Transacciones',inLastBlock:'En el último bloque',gasPrice:'Precio del gas',gas:'Gas',validators:'Validadores',finality:'Finalidad',probabilistic:'probabilística',target:'Objetivo',blockTimeChart:'Tiempo de bloque',txPerBlock:'Transacciones por bloque',cumulativeTx:'Transacciones acumuladas',gasTrend:'Precio del gas',latestBlocks:'Últimos bloques',latestTxs:'Últimas transacciones',noTxs:'Aún no hay transacciones',viewAll:'Ver todo',validator:'Validador',active:'activo',txs:'tx',txsCol:'Tx',transfer:'Transferencia',call:'Llamada',contractCreation:'Creación de contrato',searching:'Buscando…',error:'Error',copied:'Copiado',notConnected:'Ningún nodo público conectado por ahora. Los bloques y las transacciones aparecerán en cuanto la red esté conectada.',age:'Edad',txHash:'Hash',method:'Acción',from:'De',to:'A',value:'Valor',gasUsed:'Gas usado',systemContracts:'Contratos del sistema',deployedContracts:'Contratos desplegados recientemente',address:'Dirección',codeSize:'Tamaño del código',role:'Rol',brc20Note:'Las aplicaciones despliegan sus tokens mediante el estándar BRC20 de Coinbosa, compatible con ERC-20.',nativeCoinT:'Moneda nativa',tokenN:'Token',typeC:'Tipo',native:'nativa',brc20Tokens:'Tokens BRC20',noTokens:'No se detectó ningún token BRC20 desplegado. Aparecerán aquí en cuanto una aplicación despliegue uno.',valNote:'La red apunta a 12 validadores identificados. La capa de staking y la finalidad rápida están en desarrollo.',comingSoon:'próximamente',defiDesc:'El exchange del ecosistema Coinbosa — mercados spot y de futuros — está en construcción. Los mercados, la liquidez y los pares aparecerán aquí una vez que NextFuture se conecte a la cadena.',block:'bloque',transaction:'transacción',balance:'saldo',nonce:'nonce',kind:'tipo',contract:'contrato',externalAccount:'cuenta externa',bytecodeSize:'tamaño del bytecode',bytes:'bytes',unrecognized:'Formato no reconocido',footer:'red abierta, pública, con validadores autorizados'},
 pt:{status:'Estado',confirmed:'Confirmada',failed:'Falhou',fee:'Taxa',timestamp:'Data',rawData:'Dados brutos',notFound:'Não encontrado',sealer:'Selado por',position:'Posição',label:'Português',locale:'pt',explorer:'Explorador',footerDesc:'Explorador de blocos, transações e contratos da Coinbosa Chain.',ecosystem:'Ecossistema',community:'Comunidade',resources:'Recursos',readWhitepaper:'Ler o whitepaper',website:'Site oficial',deployer:'Implementador',tokensLead:'Os tokens criados pelos utilizadores com o padrão BRC20 aparecem aqui.',symbol:'Símbolo',heroTitle:'Explorador da blockchain Coinbosa',search:'Pesquisar',searchPlaceholder:'Bloco, hash de transação ou endereço 0x…',overview:'Visão geral',blocks:'Blocos',contracts:'Contratos',tokens:'Tokens',analytics:'Analytics',defi:'DeFi',online:'online',offline:'offline',stalled:'parada ({n} s sem bloco)',supply:'Oferta BOSA',decimals:'Decimais',emission:'Emissão',none:'nenhuma',latestBlock:'Último bloco',blockTime:'Tempo de bloco',transactions:'Transações',inLastBlock:'No último bloco',gasPrice:'Preço do gás',gas:'Gás',validators:'Validadores',finality:'Finalidade',probabilistic:'probabilística',target:'Meta',blockTimeChart:'Tempo de bloco',txPerBlock:'Transações por bloco',cumulativeTx:'Transações acumuladas',gasTrend:'Preço do gás',latestBlocks:'Últimos blocos',latestTxs:'Últimas transações',noTxs:'Ainda não há transações',viewAll:'Ver tudo',validator:'Validador',active:'ativo',txs:'tx',txsCol:'Tx',transfer:'Transferência',call:'Chamada',contractCreation:'Criação de contrato',searching:'A pesquisar…',error:'Erro',copied:'Copiado',notConnected:'Nenhum nó público ligado por agora. Os blocos e as transações aparecerão assim que a rede for ligada.',age:'Idade',txHash:'Hash',method:'Ação',from:'De',to:'Para',value:'Valor',gasUsed:'Gás usado',systemContracts:'Contratos de sistema',deployedContracts:'Contratos implantados recentemente',address:'Endereço',codeSize:'Tamanho do código',role:'Função',brc20Note:'As aplicações implantam os seus tokens pelo padrão BRC20 da Coinbosa, compatível com ERC-20.',nativeCoinT:'Moeda nativa',tokenN:'Token',typeC:'Tipo',native:'nativa',brc20Tokens:'Tokens BRC20',noTokens:'Nenhum token BRC20 implantado detetado. Aparecerão aqui assim que uma aplicação implantar um.',valNote:'A rede visa 12 validadores identificados. A camada de staking e a finalidade rápida estão em desenvolvimento.',comingSoon:'em breve',defiDesc:'A bolsa do ecossistema Coinbosa — mercados à vista e de futuros — está em construção. Os mercados, a liquidez e os pares aparecerão aqui assim que a NextFuture for ligada à cadeia.',block:'bloco',transaction:'transação',balance:'saldo',nonce:'nonce',kind:'tipo',contract:'contrato',externalAccount:'conta externa',bytecodeSize:'tamanho do bytecode',bytes:'bytes',unrecognized:'Formato não reconhecido',footer:'rede aberta, pública, com validadores autorizados'},
 ar:{status:'الحالة',confirmed:'مؤكدة',failed:'فشلت',fee:'الرسوم',timestamp:'الوقت',rawData:'البيانات الخام',notFound:'غير موجود',sealer:'مختوم بواسطة',position:'الموضع',label:'العربية',locale:'ar',rtl:true,explorer:'المستكشف',footerDesc:'مستكشف الكتل والمعاملات والعقود لشبكة كوينبوسا.',ecosystem:'النظام البيئي',community:'المجتمع',resources:'الموارد',readWhitepaper:'اقرأ الورقة البيضاء',website:'الموقع الرسمي',deployer:'الناشر',tokensLead:'تظهر هنا الرموز التي أنشأها المستخدمون عبر معيار BRC20.',symbol:'الرمز',heroTitle:'مستكشف بلوكشين كوينبوسا',search:'بحث',searchPlaceholder:'كتلة أو بصمة معاملة أو عنوان 0x…',overview:'نظرة عامة',blocks:'الكتل',contracts:'العقود',tokens:'الرموز',analytics:'التحليلات',defi:'التمويل اللامركزي',online:'متصل',offline:'غير متصل',stalled:'متوقفة ({n} ث بلا كتلة)',supply:'إجمالي BOSA',decimals:'المنازل العشرية',emission:'الإصدار',none:'لا يوجد',latestBlock:'آخر كتلة',blockTime:'زمن الكتلة',transactions:'المعاملات',inLastBlock:'في آخر كتلة',gasPrice:'سعر الغاز',gas:'الغاز',validators:'المُصادِقون',finality:'النهائية',probabilistic:'احتمالية',target:'الهدف',blockTimeChart:'زمن الكتلة',txPerBlock:'المعاملات لكل كتلة',cumulativeTx:'المعاملات التراكمية',gasTrend:'سعر الغاز',latestBlocks:'أحدث الكتل',latestTxs:'أحدث المعاملات',noTxs:'لا توجد معاملات بعد',viewAll:'عرض الكل',validator:'مُصادِق',active:'نشط',txs:'معاملة',txsCol:'معاملات',transfer:'تحويل',call:'استدعاء',contractCreation:'إنشاء عقد',searching:'جارٍ البحث…',error:'خطأ',copied:'نُسخ',notConnected:'لا توجد عقدة عامة متصلة بعد. ستظهر الكتل والمعاملات فور اتصال الشبكة.',age:'العمر',txHash:'البصمة',method:'الإجراء',from:'من',to:'إلى',value:'القيمة',gasUsed:'الغاز المستهلك',systemContracts:'عقود النظام',deployedContracts:'العقود المنشورة حديثًا',address:'العنوان',codeSize:'حجم الشيفرة',role:'الدور',brc20Note:'تنشر التطبيقات رموزها عبر معيار BRC20 من كوينبوسا، المتوافق مع ERC-20.',nativeCoinT:'العملة الأصلية',tokenN:'الرمز',typeC:'النوع',native:'أصلي',brc20Tokens:'رموز BRC20',noTokens:'لم يُكتشف أي رمز BRC20 منشور. ستظهر هنا حالما ينشر تطبيق ما رمزًا.',valNote:'تستهدف الشبكة 12 مُصادِقًا معرَّفًا. طبقة الحصص والنهائية السريعة قيد التطوير.',comingSoon:'قريبًا',defiDesc:'منصة تبادل نظام كوينبوسا — أسواق فورية وعقود آجلة — قيد الإنشاء. ستظهر الأسواق والسيولة والأزواج هنا بمجرد ربط NextFuture بالسلسلة.',block:'كتلة',transaction:'معاملة',balance:'الرصيد',nonce:'الرقم',kind:'النوع',contract:'عقد',externalAccount:'حساب خارجي',bytecodeSize:'حجم الشيفرة',bytes:'بايت',unrecognized:'صيغة غير معروفة',footer:'شبكة مفتوحة وعامة بمُصادِقين معتمدين'},
 zh:{status:'状态',confirmed:'已确认',failed:'失败',fee:'手续费',timestamp:'时间',rawData:'原始数据',notFound:'未找到',sealer:'出块者',position:'区块内位置',label:'中文',locale:'zh-CN',explorer:'浏览器',footerDesc:'Coinbosa 链的区块、交易与合约浏览器。',ecosystem:'生态',community:'社区',resources:'资源',readWhitepaper:'阅读白皮书',website:'官方网站',deployer:'部署者',tokensLead:'用户通过 BRC20 标准创建的代币显示在这里。',symbol:'符号',heroTitle:'Coinbosa 区块链浏览器',search:'搜索',searchPlaceholder:'区块、交易哈希或地址 0x…',overview:'概览',blocks:'区块',contracts:'合约',tokens:'代币',analytics:'分析',defi:'DeFi',online:'在线',offline:'离线',stalled:'已停止（{n} 秒无新区块）',supply:'BOSA 供应量',decimals:'小数位',emission:'增发',none:'无',latestBlock:'最新区块',blockTime:'出块时间',transactions:'交易',inLastBlock:'最新区块内',gasPrice:'Gas 价格',gas:'Gas',validators:'验证者',finality:'最终性',probabilistic:'概率性',target:'目标',blockTimeChart:'出块时间',txPerBlock:'每区块交易数',cumulativeTx:'累计交易',gasTrend:'Gas 价格',latestBlocks:'最新区块',latestTxs:'最新交易',noTxs:'暂无交易',viewAll:'查看全部',validator:'验证者',active:'活跃',txs:'笔',txsCol:'交易',transfer:'转账',call:'调用',contractCreation:'合约创建',searching:'搜索中…',error:'错误',copied:'已复制',notConnected:'尚未连接公共节点。网络接入后，区块与交易将在此显示。',age:'时间',txHash:'哈希',method:'动作',from:'从',to:'至',value:'金额',gasUsed:'消耗 Gas',systemContracts:'系统合约',deployedContracts:'最近部署的合约',address:'地址',codeSize:'代码大小',role:'作用',brc20Note:'应用通过 Coinbosa 的 BRC20 标准（兼容 ERC-20）部署代币。',nativeCoinT:'原生币',tokenN:'代币',typeC:'类型',native:'原生',brc20Tokens:'BRC20 代币',noTokens:'未检测到已部署的 BRC20 代币。一旦有应用部署，将在此显示。',valNote:'网络目标为 12 个已识别验证者。质押层与快速最终性正在开发中。',comingSoon:'即将推出',defiDesc:'Coinbosa 生态的交易所 NextFuture（现货与合约）正在建设中。一旦接入链上，市场、流动性和交易对将在此显示。',block:'区块',transaction:'交易',balance:'余额',nonce:'nonce',kind:'类型',contract:'合约',externalAccount:'外部账户',bytecodeSize:'字节码大小',bytes:'字节',unrecognized:'无法识别的格式',footer:'开放、公共、授权验证者网络'},
};

const SYS=[['0x0000000000000000000000000000000000001000','CoinbosaValidatorSet','consensus'],
['0x0000000000000000000000000000000000001001','SlashIndicator','slashing'],
['0x0000000000000000000000000000000000001002','SystemReward','reward'],
['0x0000000000000000000000000000000000001006','RelayerHub','relayer'],
['0x0000000000000000000000000000000000001007','GovHub','governance'],
['0x0000000000000000000000000000000000001008','TokenManager','tokens'],
['0x0000000000000000000000000000000000002000','CrossChain','cross-chain']];

// Point d'accès RPC : TOUJOURS same-origin (/rpc). Le serveur web relaie /rpc vers le
// nœud local ; le port 8545 reste fermé au pare-feu et la CSP peut rester stricte
// (connect-src 'self'). Une URL d'un autre hôte ou d'un autre port serait bloquee par
// la CSP : ne pas pointer directement un noeud distant ici.
// Le paramètre ?rpc= n'est accepté qu'en local (127.0.0.1) et seulement en http(s), pour
// le développement : on ne laisse pas une URL arbitraire piloter les requêtes en production.
function resolveRpc(){
  const p=new URLSearchParams(location.search).get('rpc');
  if(p&&/^https?:\/\//.test(p)&&/^(127\.0\.0\.1|localhost|\[::1\])$/.test(location.hostname)) return p;
  return location.origin+'/rpc';
}
const RPC=resolveRpc();

// ───────────────────────────────────────────────────────────────────────────
//  LIENS — modifiez ces valeurs pour mettre à jour les réseaux sociaux et le
//  livre blanc. Une valeur vide ('') masque le lien. C'est le seul endroit à
//  éditer ; le pied de page se reconstruit automatiquement.
// ───────────────────────────────────────────────────────────────────────────
const LINKS = {
  website:            'https://coinbosa.com',
  explorer:           'https://explorer.coinbosa.com',
  // Pointer vers la page PUBLIÉE, jamais vers une branche du dépôt. Le lien visait
  // master, dont le livre blanc annonce encore « preuve d'enjeu » — alors que la chaîne
  // fonctionne en preuve d'AUTORITÉ — et nie l'existence du point d'accès public, qui
  // existe. Un visiteur cliquant depuis l'explorateur lisait donc une version fausse.
  whitepaper:         'https://coinbosa.com/whitepaper/',
  github:             'https://github.com/Coinbosa/coinbosa-chain',
  facebook:           'https://www.facebook.com/coinbosa',
  twitter:            '',                               // à remplir
  telegram:           'https://t.me/coinbosa',          // groupe @coinbosa
  telegramCommunity:  'https://t.me/Coinbosaofficial',     // communauté
  discord:            '',                               // à remplir
  // produits de l'écosystème (liens à compléter quand ils ouvrent)
  academy:            '', nextfuture: '', card: '', vpn: '',
};
const ICON = {
  facebook:'<svg viewBox="0 0 24 24"><path d="M13.5 21v-8h2.7l.4-3.1h-3.1V7.9c0-.9.3-1.5 1.6-1.5h1.6V3.6c-.3 0-1.3-.1-2.4-.1-2.4 0-4 1.5-4 4.1v2.3H7.9V13h2.4v8h3.2z"/></svg>',
  twitter:'<svg viewBox="0 0 24 24"><path d="M17.5 3h3.2l-7 8 8.3 10.9h-6.5l-5.1-6.6-5.8 6.6H1.4l7.5-8.6L.8 3h6.6l4.6 6.1L17.5 3zm-1.1 16h1.8L7.6 4.8H5.6L16.4 19z"/></svg>',
  github:'<svg viewBox="0 0 24 24"><path d="M12 2C6.5 2 2 6.6 2 12.3c0 4.5 2.9 8.3 6.8 9.7.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.4-3.4-1.4-.5-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.6 2.4 1.1 3 .8.1-.7.4-1.1.6-1.4-2.2-.3-4.6-1.1-4.6-5 0-1.1.4-2 1-2.7-.1-.3-.5-1.3.1-2.7 0 0 .8-.3 2.7 1a9.3 9.3 0 015 0c1.9-1.3 2.7-1 2.7-1 .6 1.4.2 2.4.1 2.7.6.7 1 1.6 1 2.7 0 3.9-2.3 4.7-4.6 5 .4.3.7.9.7 1.9v2.8c0 .3.2.6.7.5A10.1 10.1 0 0022 12.3C22 6.6 17.5 2 12 2z"/></svg>',
  telegram:'<svg viewBox="0 0 24 24"><path d="M21.9 4.3l-3.3 15.6c-.2 1.1-.9 1.3-1.8.8l-5-3.7-2.4 2.3c-.3.3-.5.5-1 .5l.4-5 9.1-8.2c.4-.4-.1-.6-.6-.2L6 13.4l-4.9-1.5c-1.1-.3-1.1-1 .2-1.5l19.2-7.4c.9-.3 1.7.2 1.4 1.3z"/></svg>',
  discord:'<svg viewBox="0 0 24 24"><path d="M20 4.4A19 19 0 0015.4 3l-.3.5a17 17 0 014 2 15.9 15.9 0 00-13.9 0 17 17 0 014-2L8.9 3A19 19 0 004 4.4C1.6 8 1 11.5 1.3 15a19 19 0 005.8 2.9l.7-1.1c-.6-.2-1.2-.5-1.7-.8l.4-.3a13.6 13.6 0 0011.6 0l.4.3c-.6.3-1.1.6-1.7.8l.7 1.1a19 19 0 005.8-2.9c.4-4.1-.6-7.6-3-11zM8.5 13.1c-.9 0-1.7-.9-1.7-1.9s.7-1.9 1.7-1.9 1.7.9 1.7 1.9-.8 1.9-1.7 1.9zm7 0c-.9 0-1.7-.9-1.7-1.9s.8-1.9 1.7-1.9 1.7.9 1.7 1.9-.8 1.9-1.7 1.9z"/></svg>',
};
function renderFooter(){
  const soc=document.getElementById('fsoc'); if(!soc) return;
  const socials=['facebook','twitter','telegram','discord','github'];
  soc.innerHTML=socials.filter(k=>LINKS[k]).map(k=>`<a href="${esc(safeUrl(LINKS[k]))}" target="_blank" rel="noopener noreferrer" title="${esc(k)}">${ICON[k]}</a>`).join('');
  const link=(url,label)=>url?`<a href="${esc(safeUrl(url))}" target="_blank" rel="noopener noreferrer">${esc(label)}</a>`:'';
  document.getElementById('fcol-eco').innerHTML=[link(LINKS.website,'Coinbosa.com'),link(LINKS.academy||'#','Coinbosa Academy'),link(LINKS.nextfuture||'#','NextFuture'),link(LINKS.card||'#','Coinbosa Card'),link(LINKS.vpn||'#','Coinbosa VPN')].join('');
  document.getElementById('fcol-com').innerHTML=[link(LINKS.telegram,'Telegram'),link(LINKS.telegramCommunity,'Telegram '+t('community')),link(LINKS.facebook,'Facebook'),link(LINKS.discord||'','Discord'),link(LINKS.twitter||'','X / Twitter')].join('');
  document.getElementById('fcol-res').innerHTML=[link(LINKS.whitepaper,t('readWhitepaper')),link(LINKS.github,'GitHub'),link(LINKS.explorer,t('explorer'))].join('');
}
let lang=localStorage.getItem('coinbosa.lang')||(navigator.languages||[]).map(l=>l.slice(0,2)).find(l=>I18N[l])||'fr';
const t=k=>(I18N[lang]&&I18N[lang][k])||I18N.en[k]||k;

const sel=document.getElementById('lang');
for(const [c,cfg] of Object.entries(I18N)){const o=document.createElement('option');o.value=c;o.textContent=cfg.label;o.selected=c===lang;sel.appendChild(o);}
sel.onchange=e=>{lang=e.target.value;applyLang();};
function applyLang(){const cfg=I18N[lang];document.documentElement.lang=cfg.locale;document.documentElement.dir=cfg.rtl?'rtl':'ltr';
  for(const el of document.querySelectorAll('[data-i18n]')) el.textContent=t(el.dataset.i18n);
  for(const el of document.querySelectorAll('[data-i18n-ph]')) el.placeholder=t(el.dataset.i18nPh);
  localStorage.setItem('coinbosa.lang',lang);renderFooter();renderAll();}

const theme=document.getElementById('theme');
function curTheme(){return document.documentElement.dataset.theme||(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');}
theme.onclick=()=>{const n=curTheme()==='dark'?'light':'dark';document.documentElement.dataset.theme=n;theme.textContent=n==='dark'?'☀':'☾';localStorage.setItem('coinbosa.theme',n);renderAll();};
(function(){const s=localStorage.getItem('coinbosa.theme');if(s)document.documentElement.dataset.theme=s;theme.textContent=curTheme()==='dark'?'☀':'☾';})();

let toastT;function toast(m){const el=document.getElementById('toast');el.textContent=m;el.classList.add('on');clearTimeout(toastT);toastT=setTimeout(()=>el.classList.remove('on'),1400);}
window.copy=x=>{navigator.clipboard?.writeText(x).then(()=>toast(t('copied')));};

/* onglets / vues */
let view=(location.hash||'#apercu').slice(1); if(!document.getElementById('v-'+view)) view='apercu';
function showView(v){view=v;location.hash=v;
  // Changer d'onglet referme le résultat de recherche. Il se superpose aux vues :
  // laissé ouvert, il masquait le contenu de TOUS les onglets, si bien qu'Aperçu,
  // Blocs, Transactions et Contrats semblaient afficher la même chose.
  var _r=document.getElementById('result'); if(_r) _r.classList.add('hide');
  for(const el of document.querySelectorAll('.view')) el.classList.toggle('on',el.id==='v-'+v);
  for(const a of document.querySelectorAll('#tabs a')) a.classList.toggle('on',a.dataset.v===v);
  renderAll();}
document.addEventListener('click',e=>{const a=e.target.closest('[data-v]');if(a){e.preventDefault();showView(a.dataset.v);}});
addEventListener('hashchange',()=>{const v=location.hash.slice(1);if(document.getElementById('v-'+v)&&v!==view)showView(v);});

async function rpc(m,p=[]){const r=await fetch(RPC,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',method:m,params:p,id:1})});const j=await r.json();if(j.error)throw new Error(j.error.message);return j.result;}
const hex=h=>parseInt(h,16);
const nf=()=>new Intl.NumberFormat(I18N[lang].locale);
const short=(s,n=8)=>{s=String(s==null?'':s);return s?esc(s.slice(0,n)+'…'+s.slice(-6)):'';};
const coinv=h=>{try{return Number(BigInt(h||'0x0')/10n**12n)/1e6;}catch(e){return 0;}};
const ago=ts=>{const d=Math.max(0,Math.floor(Date.now()/1000)-ts);const r=new Intl.RelativeTimeFormat(I18N[lang].locale,{numeric:'auto'});return d<60?r.format(-d,'second'):r.format(-Math.floor(d/60),'minute');};
const cssv=n=>getComputedStyle(document.documentElement).getPropertyValue(n).trim();
// Échappement HTML : name()/symbol() d'un contrat sont contrôlés par le déployeur.
// Sans cela, un jeton dont name()="<img src=x onerror=…>" exécuterait du script chez le visiteur.
const esc=s=>String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
// Validateur hex pour les contextes onclick (une valeur RPC dans une chaîne JS inline :
// esc() ne protège pas, les entités HTML étant redécodées avant l'exécution du JS).
const hx=s=>{s=String(s==null?'':s);return /^0x[0-9a-fA-F]{0,64}$/.test(s)?s:'';};
// N'autorise en href qu'un schéma sûr (http/https, chemin relatif, ancre).
const safeUrl=u=>{u=String(u==null?'':u);return /^(https?:\/\/|\/|#)/.test(u)?u:'#';};

let state={blocks:[],txs:[],vals:[],sys:{},contracts:{}};
const decStr=h=>{try{const b=h.slice(2);const off=parseInt(b.slice(0,64),16)*2;const len=parseInt(b.slice(off,off+64),16)*2;let out='';const hx=b.slice(off+64,off+64+len);for(let i=0;i<hx.length;i+=2)out+=String.fromCharCode(parseInt(hx.substr(i,2),16));return out;}catch(e){return '';}};
// découvre les contrats créés par les utilisateurs et reconnaît les jetons BRC20
async function discover(){
  const creations=state.txs.filter(x=>!x.to).slice(0,12);
  for(const x of creations){
    if(state.contracts[x.hash]!==undefined) continue;
    try{
      const rc=await rpc('eth_getTransactionReceipt',[x.hash]);
      const addr=rc&&rc.contractAddress; if(!addr){state.contracts[x.hash]=null;continue;}
      const c={address:addr,creator:x.from,tx:x.hash,ts:x.ts,isToken:false};
      try{ // détection BRC20 : symbol() + decimals() + totalSupply()
        const [sym,dec,sup,nam]=await Promise.all([
          rpc('eth_call',[{to:addr,data:'0x95d89b41'},'latest']).catch(()=>null),
          rpc('eth_call',[{to:addr,data:'0x313ce567'},'latest']).catch(()=>null),
          rpc('eth_call',[{to:addr,data:'0x18160ddd'},'latest']).catch(()=>null),
          rpc('eth_call',[{to:addr,data:'0x06fdde03'},'latest']).catch(()=>null)]);
        if(sym&&dec&&sup&&sym!=='0x'&&dec!=='0x'){ c.isToken=true; c.symbol=(decStr(sym)||'?').slice(0,24); c.name=(decStr(nam)||'?').slice(0,48);
          // decimals() vient d'un contrat QUELCONQUE : le deploiement est libre et coute quelques
          // gwei. Une valeur enorme ferait calculer 10n**enorme et figerait l'onglet du visiteur.
          // On borne a 36 (au-dela d'ERC-20, deja tres large) et on refuse ce qui n'est pas un nombre.
          const d=parseInt(dec,16); c.decimals=(Number.isFinite(d)&&d>=0&&d<=36)?d:18;
          c.supply=(BigInt(sup)/10n**BigInt(c.decimals)).toString(); }
      }catch(e){}
      state.contracts[x.hash]=c;
    }catch(e){ state.contracts[x.hash]=null; }
  }
}
function txKind(x){if(!x.to)return{c:'cc',k:t('contractCreation')};if(!x.input||x.input==='0x')return{c:'tf',k:t('transfer')};return{c:'',k:t('call')};}

function areaChart(id,vals,fmt,hue){const svg=document.getElementById(id);if(!svg)return;if(vals.length<2){svg.innerHTML='';return;}
  const W=320,H=120,pl=4,pr=4,pt=8,pb=12,iw=W-pl-pr,ih=H-pt-pb,c=hue||cssv('--link');
  const max=Math.max(...vals)*1.15||1;const X=i=>pl+i/(vals.length-1)*iw;const Y=v=>pt+ih-v/max*ih;
  let d='';vals.forEach((v,i)=>{d+=(i?'L':'M')+X(i).toFixed(1)+' '+Y(v).toFixed(1)+' ';});
  const a=d+`L${X(vals.length-1).toFixed(1)} ${pt+ih} L${X(0).toFixed(1)} ${pt+ih} Z`;
  const grid=[.25,.5,.75,1].map(f=>`<line x1="${pl}" y1="${(pt+ih-f*ih).toFixed(1)}" x2="${W-pr}" y2="${(pt+ih-f*ih).toFixed(1)}" stroke="${cssv('--grid')}" stroke-width="1"/>`).join('');
  const dots=vals.map((v,i)=>`<circle cx="${X(i).toFixed(1)}" cy="${Y(v).toFixed(1)}" r="6" fill="transparent"><title>${fmt(v)}</title></circle>`).join('');
  svg.innerHTML=`${grid}<path d="${a}" fill="${c}" fill-opacity="0.12"/><path d="${d}" fill="none" stroke="${c}" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><circle cx="${X(vals.length-1).toFixed(1)}" cy="${Y(vals[vals.length-1]).toFixed(1)}" r="3" fill="${c}"/>${dots}`;}
function barChart(id,vals,fmt,hue){const svg=document.getElementById(id);if(!svg)return;if(!vals.length){svg.innerHTML='';return;}
  const W=320,H=120,pl=4,pr=4,pt=8,pb=12,iw=W-pl-pr,ih=H-pt-pb,c=hue||cssv('--brand');
  const max=Math.max(...vals,1),n=vals.length,gap=3,bw=Math.max(2,(iw-gap*(n-1))/n);
  const grid=[.5,1].map(f=>`<line x1="${pl}" y1="${(pt+ih-f*ih).toFixed(1)}" x2="${W-pr}" y2="${(pt+ih-f*ih).toFixed(1)}" stroke="${cssv('--grid')}" stroke-width="1"/>`).join('');
  const bars=vals.map((v,i)=>{const h=v/max*ih,x=pl+i*(bw+gap),y=pt+ih-h;return `<rect x="${x.toFixed(1)}" y="${y.toFixed(1)}" width="${bw.toFixed(1)}" height="${Math.max(h,1).toFixed(1)}" rx="2" fill="${c}"><title>${fmt(v)}</title></rect>`;}).join('');
  svg.innerHTML=grid+bars;}

function renderAll(){renderStats();renderCharts();renderTables();renderVals();renderContracts();renderAnalytics();}
function renderStats(){const B=state.blocks;if(!B.length)return;const head=hex(B[0].number);
  document.getElementById('s-block').textContent=nf().format(head);
  document.getElementById('s-epoch').textContent=nf().format(Math.floor(head/200));
  document.getElementById('s-txlast').textContent=nf().format(B[0].transactions.length);
  // Cette tuile compte les transactions des blocs CHARGES (18 au plus), pas celles du
  // reseau. L'afficher sans le dire laissait croire a un total reseau.
  document.getElementById('s-txs').textContent=nf().format(state.txs.length)+(state.txs.length>=40?'+':'');
  document.getElementById('s-vals').textContent=nf().format(state.vals.length||new Set(B.map(b=>b.miner)).size);}
function btvals(){const B=[...state.blocks].reverse(),bt=[];for(let i=1;i<B.length;i++)bt.push(hex(B[i].timestamp)-hex(B[i-1].timestamp));return bt;}
function renderCharts(){const B=[...state.blocks].reverse();if(B.length<2)return;const bt=btvals(),tb=B.map(b=>b.transactions.length);
  areaChart('bt-svg',bt,v=>v+' s');barChart('tb-svg',tb,v=>v+' tx');
  document.getElementById('bt-cur').textContent=(bt[bt.length-1]||0)+' s';document.getElementById('tb-cur').textContent=nf().format(tb[tb.length-1]||0);
  document.getElementById('bt-rng').textContent=bt.length+' '+t('block')+'s';document.getElementById('tb-rng').textContent=tb.length+' '+t('block')+'s';}
function renderAnalytics(){if(view!=='analytics')return;const B=[...state.blocks].reverse();if(B.length<2)return;
  const bt=btvals(),tb=B.map(b=>b.transactions.length);let s=0;const cum=tb.map(v=>s+=v);const gas=B.map(b=>hex(b.baseFeePerGas||'0x0')/1e9||1);
  areaChart('a1-svg',bt,v=>v+' s');barChart('a2-svg',tb,v=>v+' tx');areaChart('a3-svg',cum,v=>nf().format(v)+' tx',cssv('--ok'));barChart('a4-svg',gas.map(g=>g||1),v=>v.toFixed(2)+' gwei',cssv('--brand'));
  document.getElementById('a1-cur').textContent=(bt[bt.length-1]||0)+' s';document.getElementById('a2-cur').textContent=nf().format(tb[tb.length-1]||0);
  document.getElementById('a3-cur').textContent=nf().format(cum[cum.length-1]||0);document.getElementById('a4-cur').textContent=(gas[gas.length-1]||1).toFixed(1)+' gwei';
  ['a1','a2','a3','a4'].forEach(p=>{const e=document.getElementById(p+'-rng');if(e)e.textContent=B.length+' '+t('block')+'s';});}
function renderTables(){
  document.getElementById('blocks').innerHTML=state.blocks.slice(0,14).map(b=>`<div class="item"><div class="glyph">Bk</div><div><div class="l1"><a data-lookup="${hex(b.number)}">${nf().format(hex(b.number))}</a></div><div class="l2">${t('validator')} <span class="mono">${short(b.miner)}</span></div></div><div class="r"><div class="l1"><span class="badge">${nf().format(b.transactions.length)} ${t('txs')}</span></div><div class="l2">${ago(hex(b.timestamp))}</div></div></div>`).join('')||`<div class="empty">—</div>`;
  document.getElementById('txlist').innerHTML=state.txs.length?state.txs.slice(0,14).map(x=>{const k=txKind(x);return `<div class="item"><div class="glyph">Tx</div><div><div class="l1 mono"><a data-lookup="${hx(x.hash)}">${short(x.hash)}</a></div><div class="l2"><span class="badge ${k.c}">${k.k}</span> <span class="mono">${short(x.from)} → ${x.to?short(x.to):'—'}</span></div></div><div class="r"><div class="l1 tnum">${nf().format(coinv(x.value))} BOSA</div><div class="l2">${ago(x.ts)}</div></div></div>`;}).join(''):`<div class="empty">${t('noTxs')}</div>`;
  document.getElementById('blocs-body').innerHTML=state.blocks.map(b=>`<tr><td><a data-lookup="${hex(b.number)}" class="tnum">${nf().format(hex(b.number))}</a></td><td>${ago(hex(b.timestamp))}</td><td class="tnum">${nf().format(b.transactions.length)}</td><td class="mono">${short(b.miner)}</td><td class="tnum">${nf().format(hex(b.gasUsed||'0x0'))}</td></tr>`).join('')||`<tr><td colspan="5" class="empty">—</td></tr>`;
  document.getElementById('txs-body').innerHTML=state.txs.length?state.txs.map(x=>{const k=txKind(x);return `<tr><td class="mono"><a data-lookup="${hx(x.hash)}">${short(x.hash)}</a><button class="copy" data-copy="${hx(x.hash)}">⧉</button></td><td><span class="badge ${k.c}">${k.k}</span></td><td class="tnum"><a data-lookup="${hex(x.blockNumber)}">${nf().format(hex(x.blockNumber))}</a></td><td class="mono">${short(x.from)}</td><td class="mono">${x.to?short(x.to):'—'}</td><td class="tnum">${nf().format(coinv(x.value))} BOSA</td><td>${ago(x.ts)}</td></tr>`;}).join(''):`<tr><td colspan="7" class="empty">${t('noTxs')}</td></tr>`;}
function renderVals(){const V=state.vals.length?state.vals:[...new Set(state.blocks.map(b=>b.miner))];
  document.getElementById('vlist').innerHTML=V.length?V.map((v,i)=>`<div class="vcard"><div class="av">V${i+1}</div><div><div class="vm mono">${short(v,10)}</div><div class="vs">${t('validator')} #${i+1}</div></div><span class="pill">${t('active')}</span></div>`).join(''):`<div class="empty">—</div>`;}
function renderContracts(){
  const sb=document.getElementById('sys-body'); if(sb) sb.innerHTML=SYS.map(([a,n,r])=>{const sz=state.sys[a];return `<tr><td style="font-weight:600">${n}</td><td class="mono"><a data-lookup="${a}">${short(a,10)}</a></td><td class="tnum">${sz==null?'—':nf().format(sz)+' '+t('bytes')}</td><td class="l2" style="color:var(--ink-3)">${r}</td></tr>`;}).join('');
  const cs=Object.values(state.contracts).filter(Boolean);
  const db=document.getElementById('deployed-body');
  if(db) db.innerHTML=cs.length?cs.map(c=>`<tr>
    <td class="mono"><a data-lookup="${hx(c.address)}">${short(c.address,10)}</a></td>
    <td>${c.isToken?`<span class="badge tf">BRC20${c.symbol?' · '+esc(c.symbol):''}</span>`:`<span class="badge">${t('contract')}</span>`}</td>
    <td class="mono">${short(c.creator)}</td>
    <td class="mono"><a data-lookup="${hx(c.tx)}">${short(c.tx)}</a></td>
    <td>${ago(c.ts)}</td></tr>`).join(''):`<tr><td colspan="5" class="empty">—</td></tr>`;
  const tks=cs.filter(c=>c.isToken);
  const tb=document.getElementById('tokens-body');
  if(tb) tb.innerHTML=tks.length?tks.map(c=>`<tr>
    <td style="font-weight:600">${esc(c.name)||'?'}</td><td class="mono">${esc(c.symbol)||'?'}</td>
    <td class="tnum">${c.decimals??'—'}</td><td class="tnum">${c.supply?nf().format(Number(c.supply)):'—'}</td>
    <td class="mono"><a data-lookup="${hx(c.address)}">${short(c.address,10)}</a></td></tr>`).join(''):`<tr><td colspan="5" class="empty">${t('noTokens')}</td></tr>`;}
window.lookup=v=>{document.getElementById('q').value=String(v);search();};

async function refresh(){
  try{
    const [bn,gp]=await Promise.all([rpc('eth_blockNumber'),rpc('eth_gasPrice')]);
    // « En ligne » ne doit pas vouloir dire « le RPC a repondu » mais « la chaine avance ».
    // Un noeud qui sert une chaine arretee repond parfaitement : sans ce controle de
    // fraicheur, l'explorateur affichait « en ligne » sur un reseau mort.
    const ageTete=Math.floor(Date.now()/1000)-hex((await rpc('eth_getBlockByNumber',[bn,false])||{}).timestamp||0);
    const fige=ageTete>60;
    document.getElementById('dot').classList.toggle('off',fige);
    document.getElementById('netstate').textContent=fige?t('stalled').replace('{n}',String(Math.floor(ageTete)))
                                                        :t('online');state.echecs=0;{const n=document.getElementById('nonode');if(n)n.hidden=true;}
    document.getElementById('hh').textContent='#'+nf().format(hex(bn));
    document.getElementById('hg').textContent=(hex(gp)/1e9).toFixed(1)+' gwei';document.getElementById('s-gas').textContent=(hex(gp)/1e9).toFixed(1)+' gwei';
    // On ne redemande QUE les blocs qu'on n'a pas deja. Redemander les 18 derniers
    // toutes les 5 s coutait ~252 requetes/minute par visiteur — de quoi declencher la
    // limite de debit du serveur contre nos propres utilisateurs, et charger le noeud
    // pour rien. En regime etabli, un seul bloc est nouveau a chaque tour.
    const head=hex(bn),nums=[];for(let i=head;i>Math.max(-1,head-18);i--)nums.push(i);
    const cache=new Map((state.blocks||[]).map(b=>[hex(b.number),b]));
    const manquants=nums.filter(n=>!cache.has(n));
    const frais=(await Promise.all(manquants.map(n=>rpc('eth_getBlockByNumber',['0x'+n.toString(16),true])))).filter(Boolean);
    frais.forEach(b=>cache.set(hex(b.number),b));
    const blocks=nums.map(n=>cache.get(n)).filter(Boolean);
    state.blocks=blocks;
    const tsr=blocks.map(b=>hex(b.timestamp)),d=[];for(let i=0;i<tsr.length-1;i++)d.push(tsr[i]-tsr[i+1]);
    if(d.length)document.getElementById('s-time').textContent=(d.reduce((a,b)=>a+b,0)/d.length).toFixed(1)+' s';
    for(const b of blocks)for(const x of b.transactions)if(!state.txs.find(y=>y.hash===x.hash))state.txs.unshift({...x,ts:hex(b.timestamp)});
    state.txs=state.txs.slice(0,40);
    try{const r=await rpc('eth_call',[{to:SYS[0][0],data:'0x4df6e0c3'},'latest']);const body=r.slice(2);const n=parseInt(body.slice(128,192),16);if(!Number.isFinite(n)||n<0||n>128)throw 0;const vs=[];for(let i=0;i<n;i++)vs.push('0x'+body.slice(192+i*64+24,192+i*64+64));if(vs.length)state.vals=vs;}catch(e){}
    if(view==='contrats')for(const [a] of SYS)if(state.sys[a]==null)try{const c=await rpc('eth_getCode',[a,'latest']);state.sys[a]=(c.length-2)/2;}catch(e){}
    if(view==='contrats'||view==='tokens') await discover();
    renderAll();
  }catch(e){
    // Tout le cycle etait dans un seul try : n'importe quel echec isole — un 429 du
    // limiteur, un 502 passager sur l'une des ~20 requetes — faisait afficher « hors ligne »
    // et « aucun noeud raccorde », c'est-a-dire une affirmation FAUSSE sur l'etat du reseau.
    // On ne declare la chaine injoignable qu'apres 3 cycles consecutifs en echec.
    state.echecs=(state.echecs||0)+1;
    if(state.echecs<3){renderAll();return;}
    document.getElementById('dot').classList.add('off');document.getElementById('netstate').textContent=t('offline');{const n=document.getElementById('nonode');if(n)n.hidden=false;}renderAll();}
}

/* ─── Rendu lisible d'un résultat ──────────────────────────────────────────
   Le panneau affichait l'objet JSON renvoyé par le nœud. C'est exact et
   illisible : quelqu'un qui arrive depuis son portefeuille veut savoir si sa
   transaction est passée, ce qu'elle a coûté et où elle est allée — pas lire
   une sérialisation. Les données brutes restent accessibles, repliées dans un
   <details> natif : la CSP interdisant tout gestionnaire inline, aucun
   JavaScript n'est nécessaire pour l'ouvrir. */
const kv=(l,v,m)=>`<div class="kvr"><span class="kvl">${esc(l)}</span><span class="kvv${m?' mono':''}">${v}</span></div>`;
const adrl=a=>a?`<a data-lookup="${hx(a)}">${esc(a)}</a><button class="copy" data-copy="${hx(a)}">⧉</button>`:'—';
const dat=ts=>{try{return new Date(hex(ts)*1000).toLocaleString(I18N[lang].locale);}catch(e){return '—';}};
const big=h=>{try{return BigInt(h||'0x0');}catch(e){return 0n;}};
const mnt=v=>{
  // Un montant nul reste « 0 ». Un montant non nul mais minuscule doit montrer
  // ses décimales, sinon l'affichage ment.
  if(!v) return '0';
  if(v < 0.001) return new Intl.NumberFormat(I18N[lang].locale,{maximumFractionDigits:9}).format(v);
  return new Intl.NumberFormat(I18N[lang].locale,{maximumFractionDigits:6}).format(v);
};
const brut=o=>`<details class="brut"><summary>${esc(t('rawData'))}</summary><pre>${esc(JSON.stringify(o,null,2))}</pre></details>`;

function carte(res){
  if(res.__tx)  return carteTx(res.__tx,res.__rc,res.__blk);
  if(res.__blk) return carteBloc(res.__blk);
  if(res.__adr) return carteAdr(res.__adr);
  return `<div class="notice">${esc(res.err||t('notFound'))}</div>`;
}

function carteTx(tx,rc,blk){
  const ok=rc&&rc.status==='0x1';
  const prix=big(rc&&rc.effectiveGasPrice?rc.effectiveGasPrice:tx.gasPrice);
  const frais=rc?Number(big(rc.gasUsed)*prix/10n**12n)/1e6:null;
  return `<div class="rtete"><div><div class="rlab">${esc(t('transaction'))}</div>`+
    `<div class="rmnt">${mnt(coinv(tx.value))} <span>BOSA</span></div></div>`+
    `<span class="badge ${ok?'ok':'ko'}">${esc(ok?t('confirmed'):t('failed'))}</span></div>`+
    kv(t('from'),adrl(tx.from),1)+
    kv(t('to'),adrl(tx.to),1)+
    kv(t('block'),`<a data-lookup="${hex(tx.blockNumber)}">${nf().format(hex(tx.blockNumber))}</a>`)+
    (blk?kv(t('timestamp'),esc(dat(blk.timestamp))):'')+
    (frais!==null?kv(t('fee'),mnt(frais)+' BOSA'):'')+
    (rc?kv(t('gasUsed'),nf().format(hex(rc.gasUsed))+' / '+nf().format(hex(tx.gas))):'')+
    kv(t('nonce'),nf().format(hex(tx.nonce)))+
    kv(t('position'),nf().format(hex(tx.transactionIndex)))+
    kv(t('txHash'),`<span class="mono">${esc(tx.hash)}</span><button class="copy" data-copy="${hx(tx.hash)}">⧉</button>`)+
    brut({transaction:tx,receipt:rc});
}

function carteBloc(b){
  const n=(b.transactions||[]).length;
  return `<div class="rtete"><div><div class="rlab">${esc(t('block'))}</div>`+
    `<div class="rmnt">#${nf().format(hex(b.number))}</div></div>`+
    `<span class="badge ok">${nf().format(n)} ${esc(t('transactions'))}</span></div>`+
    kv(t('timestamp'),esc(dat(b.timestamp)))+
    kv(t('sealer'),adrl(b.miner),1)+
    kv(t('gasUsed'),nf().format(hex(b.gasUsed))+' / '+nf().format(hex(b.gasLimit)))+
    kv(t('txHash'),`<span class="mono">${esc(b.hash)}</span><button class="copy" data-copy="${hx(b.hash)}">⧉</button>`)+
    brut(b);
}

function carteAdr(a){
  return `<div class="rtete"><div><div class="rlab">${esc(a.genre)}</div>`+
    `<div class="rmnt">${mnt(a.bal)} <span>BOSA</span></div></div></div>`+
    kv(t('address'),`<span class="mono">${esc(a.adresse)}</span><button class="copy" data-copy="${hx(a.adresse)}">⧉</button>`)+
    kv(t('nonce'),nf().format(a.nonce))+
    (a.taille!=null?kv(t('bytecodeSize'),nf().format(a.taille)+' '+esc(t('bytes'))):'')+
    brut(a.brut);
}

async function search(){const v=document.getElementById('q').value.trim(),out=document.getElementById('result'),body=document.getElementById('result-body');
  if(!v){out.classList.add('hide');return;}out.classList.remove('hide');body.textContent=t('searching');
  try{let res;
    if(/^\d+$/.test(v)){const b=await rpc('eth_getBlockByNumber',['0x'+Number(v).toString(16),true]);res=b?{__blk:b}:{err:t('notFound')};}
    else if(/^0x[0-9a-fA-F]{64}$/.test(v)){
      const tx=await rpc('eth_getTransactionByHash',[v]);
      if(tx){
        // Le reçu porte le statut et le gaz réellement consommé ; le bloc porte
        // l'horodatage. Les deux en parallèle : l'un ne dépend pas de l'autre.
        const [rc,blk]=await Promise.all([rpc('eth_getTransactionReceipt',[v]),rpc('eth_getBlockByNumber',[tx.blockNumber,false])]);
        res={__tx:tx,__rc:rc,__blk:blk};
      } else { const b=await rpc('eth_getBlockByHash',[v,true]); res=b?{__blk:b}:{err:t('notFound')}; }
    }
    else if(/^0x[0-9a-fA-F]{40}$/.test(v)){const [bal,nonce,code]=await Promise.all([rpc('eth_getBalance',[v,'latest']),rpc('eth_getTransactionCount',[v,'latest']),rpc('eth_getCode',[v,'latest'])]);const isC=code&&code!=='0x';
      res={__adr:{adresse:v,bal:coinv(bal),nonce:hex(nonce),genre:isC?t('contract'):t('externalAccount'),taille:isC?(code.length-2)/2:null,brut:{address:v,balance:bal,nonce:nonce,code:isC?code.slice(0,66)+'…':'0x'}}};}
    else res={err:t('unrecognized')};
    body.innerHTML=carte(res);
  }catch(e){body.textContent=t('error')+' : '+e.message;}}
document.getElementById('go').onclick=search;
document.getElementById('q').addEventListener('keydown',e=>{if(e.key==='Enter')search();});

// Délégation d'événements : un seul écouteur pour toute la page. Remplace les
// attributs onclick, que la CSP interdit désormais (script-src sans 'unsafe-inline').
document.addEventListener('click',function(e){
  const l=e.target.closest('[data-lookup]'); if(l){lookup(l.getAttribute('data-lookup'));return;}
  const c=e.target.closest('[data-copy]');   if(c){copy(c.getAttribute('data-copy'));return;}
  const f=e.target.closest('[data-fermer]');if(f){const el=document.getElementById(f.getAttribute('data-fermer'));if(el)el.classList.add('hide');return;}
});
// Garde anti-chevauchement : si un cycle depasse 5 s (noeud charge, limitation de debit,
// reseau lent), setInterval en lancait un second par-dessus. Les cycles s'empilaient et un
// seul onglet produisait une rafale contre le RPC — exactement ce qu'on cherche a eviter.
let cycleEnCours=false;
async function cycle(){ if(cycleEnCours) return; cycleEnCours=true; try{ await refresh(); } finally { cycleEnCours=false; } }
applyLang();showView(view);cycle();setInterval(cycle,5000);

/* ─── URL de la forme /tx/<hash>, /block/<n>, /address/<adresse> ───────────────
   Les portefeuilles construisent leurs liens « voir sur l'explorateur » sous
   cette forme : MetaMask concatène l'URL déclarée dans la liste des chaînes et
   « /tx/<hash> ». Sans ce routage, chacun de ces liens tombait sur une 404 —
   y compris ceux que MetaMask produira dès que la chaîne sera référencée.

   Caddy réécrit ces chemins vers index.html ; c'est ici qu'on récupère la valeur
   et qu'on lance la recherche existante. Le champ de recherche et le panneau de
   résultat vivent hors des onglets : il n'y a donc aucune vue à changer. */
(function routeChemin(){
  var m = location.pathname.match(/^\/(tx|block|blocs?|address|adresse)\/([^\/?#]+)\/?$/i);
  if (!m) return;
  var champ = document.getElementById('q');
  if (!champ) return;
  champ.value = decodeURIComponent(m[2]);
  search();
})();

