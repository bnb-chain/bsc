name: Get latest release version
on:
  schedule:
    - cron:  '*/5 * * * *'
jobs:
  get-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.REPO_SCOPED_TOKEN }}
          ref: 'feature/full-block-api'
      - name: Fetch release version
        run: |
          curl -sL https://api.github.com/repos/binance-chain/bsc/releases/latest | \
          jq -r ".tag_name" > release-trigger/latest.txt
      - name: Check for modified files
        id: git-check
        run: echo ::set-output name=modified::$([ -z "`git status --porcelain`" ] && echo "false" || echo "true")
      - name: Commit latest release version
        if: steps.git-check.outputs.modified == 'true'
        run: |
          git config --global user.name 'Github Actions'
          git config --global user.email ''
          git commit -am "`cat release-trigger/latest.txt` released"
          git push origin feature/full-block-api