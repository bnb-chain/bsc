name: Integration Test

on:
  push:
    branches: 
      - master
      - develop

  pull_request:
    branches:
      - master
      - develop
      - 'merge-v1.17.3*'

jobs:
  truffle-test:
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Truffle test
      run: |
        make truffle-test
